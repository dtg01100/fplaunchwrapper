name: CI

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main, develop ]

jobs:
  python-tests:
    name: Python Tests (Full Suite)
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install UV package manager
      run: |
        curl -LsSf https://astral.sh/uv/install.sh | sh
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Install dependencies
      run: |
        uv pip install --system -e .
        uv pip install --system pytest pytest-mock pytest-timeout tomli tomli-w

    - name: Run Python tests
      env:
        HOME: ${{ runner.temp }}
        XDG_CONFIG_HOME: ''
        XDG_DATA_HOME: ''
        XDG_CACHE_HOME: ''
      run: |
        python3 -m pytest tests/python/ -v --timeout=300 --timeout-method=thread

  package-validation:
    name: Package Installation Test
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Test Python package installation
      run: |
        python3 -m pip install -e .
        python3 -c "import fplaunch; print('Python package imports successfully')"
        python3 -c "from lib.generate import WrapperGenerator; print('Package functionality works')"

  lint-and-security:
    name: Lint and Security Scan
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install UV package manager
      run: |
        curl -LsSf https://astral.sh/uv/install.sh | sh
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Install linting dependencies
      run: |
        uv pip install --system ruff bandit

    - name: Run ruff linter
      run: |
        python3 -m ruff check lib/ tests/

    - name: Run bandit security scan
      run: |
        python3 -m bandit -r lib/ -c .bandit.yml -ll
